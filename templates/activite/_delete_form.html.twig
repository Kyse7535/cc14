<form method="post" action="{{ path('activite_delete', {'id': activite.id}) }}" class="text-center" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite.id) }}">
    <button class="btn btn-danger d-inline-block m-2">Delete</button>
</form>
